-- =====================================================
-- إضافة إعداد حماية POS بكلمة مرور/باركود
-- تاريخ: 2026-02-12
-- الوصف: إضافة عمود pos_has_password لجدول settings
-- =====================================================

-- إضافة العمود إذا لم يكن موجوداً
ALTER TABLE `settings` 
ADD COLUMN IF NOT EXISTS `pos_has_password` TINYINT(1) NOT NULL DEFAULT 0 
COMMENT '1 = POS محمي بباركود، 0 = POS مفتوح';

-- تحديث القيمة الافتراضية (اختياري)
-- UPDATE `settings` SET `pos_has_password` = 0 WHERE `id` = 1;

-- =====================================================
-- ملاحظات:
-- =====================================================
-- عند تفعيل هذا الإعداد (pos_has_password = 1):
-- - سيظهر overlay في صفحة POS العادية
-- - يطلب من المستخدم مسح الباركود
-- - بعد التحقق من الباركود، يتم إخفاء الـ overlay
-- - يمكن للويترز والمستخدمين المصرح لهم الدخول

-- =====================================================
-- انتهى
-- =====================================================
